{
  "devDependencies": {
    "serverless": "^1.32.0"
  },
  "dependencies": {
    "axios": "^0.18.0"
  },
  "scripts": {
    "test": "npx sls invoke -f test --data {}",
    "deploy": "npx sls deploy",
    "d": "npx sls deploy -f test",
    "t": "npm run d && npm test"
  }
}
