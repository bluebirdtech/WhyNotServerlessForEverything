{
  "devDependencies": {
    "serverless": "^1.32.0"
  },
  "dependencies": {
    "axios": "^0.18.0"
  },
  "scripts": {
    "deploy": "npx sls deploy",
    "deploy-us": "npx sls deploy --r us-east-1",
    "d": "npx sls deploy",
    "l": "npx sls invoke -f latencyTest",
    "l-us": "npx sls invoke -f latencyTest -r us-east-1",
    "c": "npx sls invoke -f coldStartTest",
    "dl": "npm run deploy && npm run l",
    "dc": "npm run deploy && npm run c"
  }
}
