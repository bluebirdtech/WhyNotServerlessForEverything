service: wnsfe-test

provider:
  name: aws
  runtime: nodejs8.10
  region: eu-west-1
  timeout: 300

  iamRoleStatements:
    - Effect: "Allow"
      Action:
        - "lambda:InvokeFunction"
      Resource: "*"

functions:
  test:
    handler: test.handler
